type: piece
id: base:bishop
name: Bishop
# Per-side artwork under assets/sprites/bishop/base/. The glyph below stays as the
# fallback for anywhere a picture cannot be drawn.
sprite: base:bishop
presentation: { glyph: B }
material: 3

moves:
  - { type: slide, dirs: diagonal, limit: unlimited }
