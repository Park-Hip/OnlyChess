"""New-engine standard-chess oracle.

The cutover retired the old runtime. ``perft.py`` supplies published FEN positions
and node counts; ``new_adapter.py`` drives the ordinary ``base:chess`` engine.
"""
